create table userInformation(firstName, familyName, email PRIMARY KEY, gender, city, country, password, passwordHash UNIQUE);
create table messages(message, senderEmail, recieverEmail);
create table loggedInUsers(token PRIMARY KEY, email UNIQUE);
